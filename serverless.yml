service: test

frameworkVersion: "3"

provider:
  name: aws
  runtime: python3.8
  region: ap-southeast-1

functions:
  hello:
    handler: main.print_hi
    layers:
      - !Ref layerOne

layers:
  layerOne:
    path: layerOne
    package:
      patterns:
        - '!env.tar.gz'